name: Auto-Assign PR

on:
  pull_request:
    types: [opened, ready_for_review]

permissions:
  pull-requests: write
  contents: read

jobs:
  assign-pr:
    name: Auto-Assign Pull Request
    runs-on: ubuntu-latest

    steps:
      - name: Assign PR to Author
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const { owner, repo } = context.repo;
            const pr = context.payload.pull_request;
            await github.rest.issues.addAssignees({
              owner,
              repo,
              issue_number: pr.number,
              assignees: [pr.user.login]
            });

      - name: Request Review from Team
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const { owner, repo } = context.repo;
            const pr = context.payload.pull_request;
            const reviewers = ['maintainer1', 'maintainer2'];
            try {
              await github.rest.pulls.requestReviewers({
                owner,
                repo,
                pull_number: pr.number,
                reviewers: reviewers.filter(r => r !== pr.user.login)
              });
            } catch (error) {
              console.log(`Could not request: ${error.message}`);
            }

      - name: Add Labels Based on Files Changed
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const { owner, repo } = context.repo;
            const pr = context.payload.pull_request;
            const { data: files } = await github.rest.pulls.listFiles({
              owner,
              repo,
              pull_number: pr.number
            });
            const labels = new Set();
            files.forEach(file => {
              if (file.filename.match(/\.js$|\.jsx$/)) {
                labels.add('javascript');
                labels.add('frontend');
              }
              if (file.filename.match(/\.css$/)) labels.add('styles');
              if (file.filename.match(/\.test\.js$/)) labels.add('tests');
              if (file.filename.match(/\.(yml|yaml)$/)) {
                labels.add('ci-cd');
              }
              if (file.filename.match(
                /package\.json|package-lock\.json/
              )) {
                labels.add('dependencies');
              }
              if (file.filename.match(/README|CHANGELOG|\.md$/)) {
                labels.add('documentation');
              }
            });
            if (labels.size > 0) {
              await github.rest.issues.addLabels({
                owner,
                repo,
                issue_number: pr.number,
                labels: Array.from(labels)
              });
            }

      - name: Post Welcome Comment
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const { owner, repo } = context.repo;
            const pr = context.payload.pull_request;
            const msg = `## Welcome @${pr.user.login}!\n\n` +
              `- Assigned to you\n` +
              `- Reviewers notified\n` +
              `- Labels added`;
            await github.rest.issues.createComment({
              owner,
              repo,
              issue_number: pr.number,
              body: msg
            });
